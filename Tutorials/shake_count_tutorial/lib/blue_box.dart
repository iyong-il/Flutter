import 'package:flutter/material.dart';
import 'package:velocity_x/velocity_x.dart';

class BlueWidget extends StatelessWidget {
  const BlueWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) =>
      Container().box.blue500.size(20, 20).make();
}
