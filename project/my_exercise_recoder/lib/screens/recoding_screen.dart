import 'package:flutter/material.dart';

class RecodingScreen extends StatelessWidget {
  const RecodingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
